# Next Task
- implement rest of the shapes
- render types of shapes from sprites according to type
- basic gameplay: spawn cycle + randomy position + random rotation
- random spawn cycle + placing
- timestep accumulated FDT interpolation + where to determine input state -> Currently double firing input states across ticks

# Goal
Fully playable version of Tetris
Similar gameplay to Tetric Classic on GameBoy Classic
Simple building for windows
> Stuff that does not matter
   - Code architecture
   - Perfect re-creation

# Gameplay Notes
> screens
   - splash
   - music type
   - game
   - death -> slash ?
> GUI
   - borders
   - field
   - score
   - level
   - lines
   - next shape
> tetro
   - rotations
   - blocking impossible rotations
   - support all tetro types
   - random type spawning in level based on seed
   - render tetros properly
   - movement left and right + input timing
   - movement jolt down with + input timing
   - full line break + flash + drop down
> juice
   - music
   - sfx

# Feature features
> limit framerate when VSYNC not supported or disabled
   - https://lazyfoo.net/tutorials/SDL3/12-frame-rate-and-vsync/index.php

# Things to improve
- adding source files, libraries, and linking to existing build process
- disable terminal running on release build